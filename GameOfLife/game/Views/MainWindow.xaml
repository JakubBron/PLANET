<Window x:Class="game.Views.MainWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:local="clr-namespace:game.Views"
            Title="Game of Life" Height="800" Width="1200">


    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="5">
            <Button Content="Start" Click="Start_Click"/>
            <Button Content="Stop" Click="Stop_Click"/>
            <Button Content="Step" Click="Step_Click"/>
            <Button Content="Clear" Click="Clear_Click"/>
            <Button Content="Random" Click="Random_Click"/>
            <Button Content="Save" Click="Save_Click"/>
            <Button Content="Load" Click="Load_Click"/>
            <TextBlock Text="Cell Size:" VerticalAlignment="Center" Margin="5,0"/>
            <TextBox Name="CellSizeBox" Width="40" Text="10" TextChanged="CellSizeBox_TextChanged"/>
            <TextBlock Text="Zoom:" VerticalAlignment="Center" Margin="5,0"/>
            <Slider Minimum="0.1" Maximum="5" Value="1" Width="100" ValueChanged="ZoomSlider_ValueChanged"/>
            <TextBlock Text="Rules:" VerticalAlignment="Center" Margin="5,0"/>
            <TextBox Name="RulesBox" Width="60" Text="B3/S23" TextChanged="RulesBox_TextChanged"/>
            <TextBlock><Run Text="Board size"/><Run Text=":"/></TextBlock>
            <TextBox x:Name="BoardSizeUserX" Width="60" Text="100" TextChanged="BoardSizeBox_XValueChanged"/>
            <TextBlock><Run Language="pl-pl" Text="x"/></TextBlock>
            <TextBox x:Name="BoardSizeUserY" Width="60" Text="100" TextChanged="BoardSizeBox_YValueChanged"/>
            <Button x:Name="ResizeButton" Content="Resize" Click="ResizeButton_OnClick"/>
        </StackPanel>

        <StackPanel DockPanel.Dock="Right" Margin="5">
            <TextBlock Text="Cell Shape:" FontWeight="Bold" Margin="0,0,0,5"/>
            <ComboBox x:Name="ShapeComboBox"
                SelectedValue="{Binding Path=CellShape}"
                SelectedValuePath="Content">
                <ComboBoxItem Content="Rectangle" IsSelected="True"/>
                <ComboBoxItem Content="Circle"/>
                <ComboBoxItem Content="Triangle"/>
            </ComboBox>
            <TextBlock Text="Cell Color:" FontWeight="Bold" Margin="0,10,0,5"/>
            <ComboBox x:Name="CellColorComboBox" SelectionChanged="CellColorComboBox_SelectionChanged">
                <ComboBoxItem Content="LimeGreen" IsSelected="True"/>
                <ComboBoxItem Content="Red"/>
                <ComboBoxItem Content="Blue"/>
                <ComboBoxItem Content="Yellow"/>
                <ComboBoxItem Content="Magenta"/>
            </ComboBox>
            <StackPanel DockPanel.Dock="Right" Margin="5">
                <TextBlock Text="Simulation Speed (ms per step):" FontWeight="Bold" Margin="0,10,0,5"/>
                <Slider x:Name="SpeedSlider" Minimum="10" Maximum="1000" Value="100" TickFrequency="50" IsSnapToTickEnabled="True" ValueChanged="SpeedSlider_ValueChanged"/>
            </StackPanel>

            <StackPanel x:Name="StatsPanel" Orientation="Vertical"
                        HorizontalAlignment="Left"
                        Background="#80000000" Margin="5" Width="178">
                <TextBlock Text="{Binding Board.Generation, StringFormat=Generation: {0}}" Foreground="Yellow" FontFamily="Consolas"/>
                <TextBlock Text="{Binding Board.Born, StringFormat=Born: {0}}" Foreground="Yellow" FontFamily="Consolas"/>
                <TextBlock Text="{Binding Board.Died, StringFormat=Died: {0}}" Foreground="Yellow" FontFamily="Consolas"/>
                <TextBlock Text="{Binding Board.Cells.Count, StringFormat=Cells: {0}}" Foreground="Yellow" FontFamily="Consolas"/>
            </StackPanel>
        </StackPanel>

        <ScrollViewer x:Name="BoardScrollViewer" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <local:BoardRenderer x:Name="BoardCanvas"/>
        </ScrollViewer>

    </DockPanel>
</Window>
